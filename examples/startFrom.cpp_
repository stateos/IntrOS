#include <stm32f4_discovery.h>
#include <os.h>

auto led = Led();
auto tsk = Task([]{});

int main()
{
	tsk_flip([]
	{
		tsk_sleepNext(SEC);
		tsk.startFrom([]
		{
			led.tick();
			ThisTask::stop();
		});
		tsk.join();
	});
}
